apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: victorialogs
resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml
# Add more resources as needed (e.g., ConfigMap for log shipping)
