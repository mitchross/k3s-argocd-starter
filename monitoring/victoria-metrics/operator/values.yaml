# VictoriaMetrics Operator Helm values for home lab
resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
# Ensure the operator watches all namespaces for CRDs (including VLogs, VLAgent, etc.)
operator:
  watchNamespace: ""
