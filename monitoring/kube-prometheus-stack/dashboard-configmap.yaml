apiVersion: v1
kind: ConfigMap
metadata:
  name: custom-dashboards
  namespace: kube-prometheus-stack
  labels:
    grafana_dashboard: "1"
data:
  # Dashboard files will be added here
  # Each .json file becomes a key in this ConfigMap
